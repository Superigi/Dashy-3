#import discord
#from discord.ext import commands

#class Example(commands.Cog):

    #def __init__(self , client):
       # self.client = client


    #event exapmle
    #@commands.Cog.listener()
    #async def on_ready(self): # self must be included in a cog if it doing something with any inputs
        #print("Bot is Online")

    

#def  setup(client):
#    client.add_cog(Example(client))